---
description: How to safely deploy the application
---

1. Run `npm run lint` in root to check for issues.
2. Run `npm run build` in `packages/frontend` to verify build.
3. // turbo
   Run `cdk deploy` (via the established npm script).
